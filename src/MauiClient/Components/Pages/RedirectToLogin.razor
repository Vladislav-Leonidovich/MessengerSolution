@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Примусове перенаправлення на сторінку логіну
        NavigationManager.NavigateTo("/login", true);
    }
}